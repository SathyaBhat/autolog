services:
  autolog:
    build: .
    container_name: autolog
    restart: unless-stopped
    env_file:
      - path: .env
    volumes:
      - autolog-data:/data
    networks:
      - dokploy-network

volumes:
  autolog-data:

networks:
  dokploy-network:
    external: true
